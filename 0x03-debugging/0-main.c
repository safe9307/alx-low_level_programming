#include"main.h"

/**
 * main - test positive_or_negative() in case of 0
 * it executes, and prints it
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
