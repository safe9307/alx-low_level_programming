#include"main.h"

/**
 * _abs - absolute value of a number
 * @n: number entry
 * it executes, and prints it
 * Return: absolute value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
